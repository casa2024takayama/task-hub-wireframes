# パーソナルタスクハブ MVP 仕様メモ

## 想定ユーザー
マルチタスクで5〜6プロジェクトが並行する人（社内イベント・社外イベント・Web・X 等）。時間が分断され、1つに集中できない働き方。

## 解決したい課題
- **メールで来た小さい依頼を忘れる**（このアプリを作ろうと思った直接のきっかけ）
- 大プロジェクトはプレッシャーで覚えているが、小さい頼まれごとが抜け落ちる
- Notion / JIRA は「タスク中心」で再開コストが高く、続かなかった

## 設計の中核（=このツールのキモ）
**アプリのトップに常駐する貼り付け欄**
着信したメールやチャットの本文をワンタップで貼り付け → 受信箱に直行 → 後で振り分け。
"捕獲の摩擦をゼロに近づけること" が生命線。これが崩れたら全てが崩れる。

## データモデル（3層）
- **プロジェクト**（種別フィールドとして単発／ルーチン週次・月次の概念あり。**ルーチン機能は当面未実装**。レポート上はグレーアウト扱い。将来復活前提）
  - **作成日**・**プロジェクト締切** を保持する想定。
  - 完了へ切り替えたときは **完了日（終了記録、`completedAt` 等／実装側の名前に合わせる）を1つ** 記録する。
  - **誤って完了したあと復帰（再度アクティブ化）するとき：** **`completedAt` のみ無効化**（完了日だけ取り消す）。**プロジェクトの締切は削除しない／保持する。**
  - 再開メモ（概要・復帰用メモ。**URL はプレーン文中の自動リンク化で十分。** Markdown 構文対応は不要）
  - **タスク**（作成日・**タスク締切**・**サイズ**：小〜約30分 / 中 / 大 / 特大：**目安時間のラベル付き**。**プッシュ通知等は「小」のみ**。**特大より大きい仕事はプロジェクトにする**運用。**依頼者**）
    - **プロジェクト締切とタスク締切は両方もてる。** タスク締切がプロジェクト締切を論理的に超えることは運用上あり得る → **入力禁止はせず警告表示のみ。**
    - **受信箱からタスク化するとき：** 捕獲した本文を **`originalPaste`（仮名・実装に合わせる）としてタスクにコピー** する（タイトル編集後も原文参照・メモ用）。
  - **アイテムリスト**（撮影小物・素材など "揃えるもの"。締切ではない）

### タスクの「依頼者」フィールド（v0.5.0 追加）
- `requestedBy?: string` — 自由入力。「△△さん」「経営会議」「自分」など何でも書ける
- 目的:
  - 復帰時に「これ誰の依頼だっけ」を思い出すコストを下げる
  - レポートで「今月、誰からの依頼を何件こなしたか」を可視化する（v0.6 で実装予定）
- 入力体験:
  - フォームに**過去入力値のサジェスト（`<datalist>`）** を出す → 表記揺れを軽減
  - 「**自分**」ボタンを置き、1 タップで `requestedBy = '自分'` を入れられる
  - AI 自動振り分けでも `requestedBy` を推測（敬称付きで統一）
- 表記揺れの正規化はしない（個人ツールなので運用で吸収。観察してから対応）

### アイデア（自分発タスク）の扱い
- 「自分のアイデア」も Task として登録する。**`requestedBy = '自分'` であることが識別子**
- 配置はプロジェクト直下が基本（案件と一緒に育てる）。横断的なものは「アイデア帳」プロジェクトを別途作る運用
- 通知は対象外（下記参照）

## 主要画面
1. **ダッシュボード（トップ）**
   - 最上段：常駐貼り付け欄
   - 中段：今日／明日の "小タスク" だけのピン留め帯
   - 下段：プロジェクトカード一覧（各カードに再開メモを表示）。並び順の既定：**プロジェクト締切が早い順**。手動並び／ドラッグで順序を変えて保存する機能は別途検討可（並び順用フィールド）。
2. **受信箱**（未振り分けの捕獲タスク）
3. **プロジェクト詳細**（タスク + アイテムリスト）
4. **週間／月間レポート**（アクティブプロジェクト本数・消化タスク数など）。**「プロジェクト別タスク消化」の見出し付近で各プロジェクトの期限を明示。** ルーチン関連の箇所は機能未実装のため **グレーアウト**（復活見込みのため削除はしない）。

## 入力経路（実装順）
1. トップの貼り付け欄 → 受信箱直行 ← まずこれだけで MVP
2. iOS ショートカット + PC ブックマークレット（共有シート経由）
3. 専用メール転送アドレス（必要になったら）

## 通知
- 当日朝＋前日に「**小タスクのみ**」（〜約30分相当のバケット）を浮上。**中／大／特大は通知しない**。
- **`requestedBy === '自分'`（＝アイデア）は通知対象外**。依頼の取りこぼし防止に集中するための設計
- iOS PWA の通知が弱ければ、LINE Bot かメール経由でフォールバック

## プラットフォーム
PWA 一本（PC ブラウザ + iPhone ホーム画面追加）。コードベースを分けない。

## MVP でやらないこと
- 全タスク締切俯瞰リスト（プロジェクトごとに締切が違うので不要）
- チームコラボ・共有
- 凝ったテンプレート機能（ルーチン複製はボタン1つだけで OK）
- ガントチャート、依存関係、ステータスのワークフロー

## 検証指標
- **自分が2週間使い続けられるか**
- 小さい依頼の取りこぼしが体感で減るか
- 続かなかったら、その脱落ポイントを観察して直す

## 開発方針
完璧に詰めず1〜2週間で雑に作る → 自分で2週間使う → 観察して直す、を1サイクル。

---

# 3階建て統合構想（2026-06-11 設計レビュー確定）

ユーザーの「3階建てタスク管理術」（1階=年間ロードマップ / 2階=案件ボード / 3階=朝のカンバン）のうち、現在タスクハブは2階のみ。1階・3階は別の単体HTML（localStorage）で運用されており二重入力が発生している（実例：IT Women Summit が3階にあって1階に無い）。タスクハブを単一データ基盤にして3階層すべてをビューとして提供する。

## 実データ検証で確定した設計判断

- **カンバンは仕事量を過少報告している**: 実週報の完了は8件/週だが、依頼は約5件/日届く。細かい依頼は即対応されて記録に残らない。受信箱の「完了済み」ボタン経由で即応分も記録し、週報サマリーに「受信箱で即応した依頼 N件」を出す
- **「待ち」には待ち先と開始日が必須**: 広報業務の約半分は他人待ち。`waitFor`（待ち先）+ `waitSince`（待ち開始日、自動記録）で「施工会社の返信4日待ち」と報告できて初めて管理者が動ける
- **常設プロジェクト型が必要**: 実データの施策の過半（他部門支援・InfoSquare・データ集計）は単発でもルーチンでもない「終わりのない機能領域」。レポートでは消化率でなく処理件数で見せる
- **回（rounds）は手動作成、カデンスエンジンは作らない**: 実データでは皇居ラン×3・MOV Meeting×4 が回ごとに手入力されており、日付は不規則（第N曜日ルールに従っていない）。「次回分を作成→基準日を選ぶ」方式で十分。規則的な MOV Radio（第2・4金）は次回候補日のサジェストに使う程度
- **ボード着地は手動引き上げが主役、締切自動着地は補助**: 実データでは日付を持つタスクは8件中1件。「締切が今週中→自動で今週やる列」は維持しつつ、「日付なし・来週以降から引き上げる」導線を主動線として設計する
- **粒度はイベント単位に統一**: 3階の施策「展示会・イベント」は粗すぎ、1階はイベント単位。プロジェクトはイベント単位（デブサミ Summer 単独）とし、週報は自然にイベント別になる
- **planned（計画中）ステータス**: 11月のイベントを6月のダッシュボードに出さないため。1階のロードマップにだけ表示され、準備開始時に手動で active 昇格（月次/四半期レビューが昇格判断の場）
- **週報フォーマットは現行を踏襲**: 「今週動いたこと（曜日分布）/進行中/詰まり・要相談/来週やること」。既に上司に通っている形式を変えない。サマリー（完了数・概算工数・即応依頼数）を上に足す
- **工数概算**: サイズ4段階×単価（小0.5h / 中2h / 大4h / 特大8h ※要ユーザー確認）で「今週 約14時間分」を出す

## データモデル変更（persist v4 想定）

```
Task    + weekStatus: 'todo' | 'doing' | 'wait' | 'done' | null   // null = ボード外（バックログ）
        + waitFor:   string | null    // 待ち先（wait のとき）
        + waitSince: string | null    // 待ちに入れた日（自動記録）
        + roundLabel: string | null   // "#16" "5月回" など回の識別

Project + status に 'planned' を追加（planned → active → completed）
        + type に '常設' を追加（単発/週次/月次/四半期/常設）
        + outputType: 'event'|'article'|'video'|'radio'|'sponsor'|'internal'|'run'|'other'  // 1階の「種類」
        + taskTemplate: { title, size, offsetDays }[]   // 回テンプレ（offsetDays は基準日からの相対日数、例: 募集開始 -14）
        + itemTemplate: string[]                         // 回ごとのアイテムリスト雛形
```

- バリデーション: プロジェクト終了日 ≥ 開始日（実データに逆転エントリがあった）

## 実装フェーズ

| バージョン | 内容 | 対応する階 |
|---|---|---|
| v0.7 | `/week` ボード（今週やる/着手中/待ち/完了）・`weekStatus`・`waitFor/waitSince`・常設型・手動引き上げ＋締切自動着地・週報生成 | 3階 |
| v0.7.5 | 回テンプレ（工程＋相対日数＋回ラベル）→ MOV Running / MOV Tube / MOV Radio | 2〜3階 |
| v0.8 | レポート強化（即応依頼カウント・工数概算・依頼者別・待ち滞留警告） | 報告 |
| v0.9 | `planned`・`outputType`・`/roadmap` 画面（1階吸収）・日付バリデーション | 1階 |

## 既存データの移行マッピング（1階 13件＋反復1件 → プロジェクト7本）

- 皇居ラン×3 → ルーチン「MOV Running」1本＋回×3
- MOVミーティング/MOV Meeting×4（表記揺れあり）→ ルーチン「MOV Meeting」（四半期）1本＋回×4
- MOV Radio配信日（第2・4金）→ 既存 MOV Radio プロジェクトに回として統合
- デブサミ Summer/KANSAI/FUKUOKA → 単発×3（Summer=active、他=planned）
- ニコニコ超会議 → 単発・completed
- IT チャリティ駅伝・赤城自然園_秋ツアー → 単発・planned

## 3階建てで「やらないこと」

- 規則カデンスの自動スケジューリング（回は手動作成）
- 旧 HTML（morning-kanban / annual-roadmap）との双方向同期（移行は一回きりのインポート）
- ルーチンの「実施率」KPI（回の完了数カウントで代替。実施率は将来）
